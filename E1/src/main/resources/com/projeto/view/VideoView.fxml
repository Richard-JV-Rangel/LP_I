<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<AnchorPane xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1"
    fx:controller="com.projeto.controler.VideoController">
    <children>
        <VBox alignment="TOP_CENTER" spacing="15.0" AnchorPane.bottomAnchor="20.0"
            AnchorPane.leftAnchor="20.0" AnchorPane.rightAnchor="20.0" AnchorPane.topAnchor="20.0">
            <children>
                <Label styleClass="title-label" text="Modelagem: Vídeo de Piano" />
                <HBox alignment="CENTER" spacing="20.0">
                    <children>
                        <VBox alignment="TOP_CENTER" spacing="10.0" styleClass="section-pane">
                            <children>
                                <Label styleClass="section-title-label" text="Classe: Pianista" />
                                <Button onAction="#handleTocarMusica" text="Executar tocarMusica()" />
                                <Button onAction="#handleImprovisar" text="Executar improvisar()" />
                                <Button onAction="#handleVirarDeCostas"
                                    text="Executar virarDeCostas()" />
                            </children>
                        </VBox>
                        <VBox alignment="TOP_CENTER" spacing="10.0" styleClass="section-pane">
                            <children>
                                <Label styleClass="section-title-label" text="Classe: Piano" />
                                <Button onAction="#handleEmitirSom" text="Executar emitirSom()" />
                                <Button onAction="#handleAbrirTampa" text="Executar abrirTampa()" />
                                <Button onAction="#handleSuportarPeso"
                                    text="Executar suportarPeso()" />
                            </children>
                        </VBox>
                        <VBox alignment="TOP_CENTER" spacing="10.0" styleClass="section-pane">
                            <children>
                                <Label styleClass="section-title-label" text="Classe: Video" />
                                <Button onAction="#handleReproduzir" text="Executar reproduzir()" />
                                <Button onAction="#handlePausar" text="Executar pausar()" />
                                <Button onAction="#handleAvancar" text="Executar avancar()" />
                            </children>
                        </VBox>
                    </children>
                </HBox>
                <Label styleClass="section-title-label" text="Console de Resultados:">
                    <VBox.margin>
                        <Insets top="10.0" />
                    </VBox.margin>
                </Label>
                <TextArea fx:id="outputArea" editable="false" prefHeight="350.0" wrapText="true" />
            </children>
        </VBox>
    </children>
</AnchorPane>